# Build yg — The Sixth Entry Sweep, 2026-09-26

Sweep header, 2026-09-26 12:31 UTC, read by gpt-5.5 on build
`2026-09-25yf-the-third-calculus-sweep`: **36 of 36 lessons read · 6 findings (5 on
generators, 1 on authored beats) · 31 lessons clean · 0 unplaced · 0 unread · 807.4s.** By
kind: untaught-term 3, false 2, unsupported 1. The floor: `wy`'s reading **19 findings, 20
clean**.

Stamp: **`2026-09-26yg-the-sixth-entry-sweep`**. PART **3oa**. A sweep build; Jim pasted the
report. With it **the third round has read nine of ten courses** — only Pre-Calc's fourth
reading is left.

## The smallest pile of any reading — and five of six on generators

Six findings, thirty-one clean. Entry went 19 → 6 (its sixth reading). Nothing declined. Only
one finding was on an authored beat; the other five were on four generator walk-backs, which
is where the third round's fixes reach every lesson sharing the op.

## The untaught symbol (3, one class)

Three walk-back boards drew a symbol Entry never teaches: `big`'s `13 > 6`, `eqs`'s
`6 ÷ 2 = 3 each`, `min5q`'s `55 ÷ 5 = 11`. Each is words now, in the shape its own ask board
already uses — `13 is bigger than 6`; `6 shared into 2 equal groups = 3 each`; `55 minutes =
11 counts of five`. PART 3oa checks that no walk-back of any Entry problem, and no authored
Entry line or board, carries `÷` or `>`.

## The count that was not shown (1, the `-` walk-back)

*"Here it is, step by step: 11, take 2 away — 9 are left"* asserted the answer. The base
subtraction walk-back — used only by Entry's three unit-3 lessons — now follows each lesson's
own method: a start under ten counts the stars still standing (*"7, take 3 away. Count the ones
still standing — 1, 2, 3, 4. 4 are left"*, the single-digit lesson's method), ten and up counts
back from the start (*"start at 11 and count back 2 — 10, 9. 11 take away 2 equals 9"*, the
bigger-numbers lesson's), and the caption carries the count. The story noun (*"each star stands
for one of the kites"*) is kept; the column paths for two-digit numbers are untouched.

## The law with its condition (1) and the one-off (1)

`min5q`: *"every clock number is worth five minutes"* — the 12 is 0 minutes — is *"each step
from one clock number to the next is five minutes"*. And *"hundreds of pages in a book"*
(not every book) is *"A big book has hundreds of pages, a big school has hundreds of
students, and a jar can hold hundreds of pennies."*

Grading under the 09-22 ruling: **class** (the untaught symbol 3, the shown count 1, the
condition 1), **one-off** (1), **reviewer** (0).

## Counts

Course lines **40,495 — unchanged**. Speechmap 941. Every lesson validates. Two pins moved
with the text (3mt's two `-` walk-back sentences).

## After the push

`/health` = `2026-09-26yg-the-sixth-entry-sweep`. **Prewarm 1 line** (the hundreds why beat;
the generator lines are cache hits already). Open Entry unit 3 lesson 2 and miss one: the
walk-back counts back out loud. One sweep left in the third round: Pre-Calc's fourth.

## Files

`lessonscripts.py` (`_col_sub`'s single-digit path, `big`, `eqs`, `min5q` walk-backs),
`lessons/entry.py` (one edit), `ruletests.py` (PART 3oa; two pins moved), `speechmap.py`
(regenerated, 941), `main.py` (stamp), this doc, the refreshed
`START_HERE_Handoff_2026-09-26.md`.

Battery on the frozen copy, 2026-09-26: **13,146 passed · 0 failed · 3 skipped**, first run clean (13,136 at `yf`).

I did no harm and this file is not truncated.
