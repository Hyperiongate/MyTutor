# Build xg — The Second Prob/Stat Sweep (2026-09-21)

Sweep header, 2026-09-21 19:22 UTC, read by gpt-5.5 on build
`2026-09-19xf-the-pre-sweep-the-other-eight`: **36 of 36 lessons read · 60 findings (27 on
generators, 33 on authored beats) · 10 lessons clean · 0 unplaced · 0 unread · 2655.4s.**
By kind: false 22, repeats 18, words-board 12, unclear 4, untaught-term 3, unsupported 1.
The floor was `wo`'s first reading: **92 findings, 1 clean**, fixed in `wq`.

Stamp: **`2026-09-21xg-the-second-probstat-sweep`**. Battery: see the bottom of this doc.

## What the pre-sweep bought, and what it missed

`xe` closed two classes in this course ahead of the reading: the closing recap board the
words never read, and the teaching sentence of 27 words or more. Both held — **not one
words-board finding landed on a closing recap, and all four "unclear" findings landed in
beat kinds the scan never covered**: a walk-back (`resp`, `sblw`), an ask (`indp`) and an
advance line. That is the measurement's next widening, noted in the handoff.

But `xe` also **created two of this sweep's HIGHs**, and they are the most useful thing in
the report:

- "And that is why the machine stays open: 10 take away 7 is 3, **every play**." — sitting
  directly under a recap that says "A play costs what you pay take away what comes back **on
  average**. One play can bounce either way."
- "And that is what **give or take means**: 46 take away 5, 41." — under a recap that says
  "The margin reaches the same distance **each way**… the low end is the estimate take away
  the margin; the high end is the same step up."

Both closing lines read their board correctly and contradicted the sentence above them.
The ruling, now pinned: **a closing credit line inherits the conditions of the recap it
closes.** If the recap says "on average", "each way" or "in the long run", the line that
reads the board says so too. They now read "10 take away 7 is 3 a play, on average" and
"And that is the low end of the band: 46 take away 5, 41."

## The big class: a praise that explains (18 of 60)

Every finding tagged `repeats` was the same shape — a praise beat that explains, and a
breath later a walk-back explaining the same thing over the same board. Six generator ops:
`dotm`, `madv`, `zval`, `cbse`, `n68`, `zsco`.

This class has been closed twice already and kept coming back. `wy` pinned the praise that
is the walk-back **word for word** (PART 3mt); `xa` pinned the praise that is the walk-back
**in other words** (PART 3mv: the same numbers in the same order, plus six words in ten
shared). Both passed these six. The reason is mechanical: **one extra sentence on either
side defeats both tests** — the number lists stop matching, and the whole-line overlap
falls under the bar.

Measuring properly showed what actually separates the ops the reviewer flags from the ones
it leaves alone, and it is not similarity at all. It is **length**:

| course | ops whose praise runs past 26 words (before this build) |
|---|---|
| Entry, Basic, Pre-Algebra | 0 |
| Algebra I / Geometry / Algebra II | 3 / 2 / 3 |
| **Prob/Stat** | **24** |
| Pre-Calc | 19 |
| Calculus | 32 |

Every op this sweep flagged had a praise over 30 words. Every credit line `xa` wrote is
under 21. The three courses whose praises were never converted are exactly the three whose
sweeps keep raising this class. So the measurement *is* the ruling — **a praise is the
answer and its one reason** — and all 24 Prob/Stat praises are credit lines now:

- `dotm`: "18 — the value UNDER the tallest stack, never the 5 readers standing on it."
- `zval`: "64 — two deviations of 2 is 4, laid on top of the mean."
- `hedg`: "3 tokens a play on average — 10 out, 7 back."
- `cwid`: "4 points wide — the margin 2 reaches below AND above the middle."

The walk-backs keep every step and every wrong-path note, so nothing was lost with the
words. Where an older pin guarded a phrase in a praise (`hedg`'s "one play can bounce
either way", `por`'s "timesing belongs to the AND rule for separate events", `zsco`'s "a
common scale"), the phrase moved into the walk-back, which says it anyway, and the pin
moved with it.

## The other truth findings

- **A chance is not a quota (HIGH).** `ppct`'s grid caption said "85 of every 100 picks
  would be red"; it now says "about 85 of every 100 picks, in the long run". The poll's
  band in U9 is what the poll can say, not where the truth sits.
- **The bell's 68 is not a law of every curve (HIGH ×2).** "The bell always shares itself
  out the same way" is gone from the teach and from the reason choice; the share belongs to
  groups that follow a bell curve.
- **A percentile is a position (HIGH).** The reason choice was "because a percentile is a
  percent of the group" — which is what a percentile is *not*. It now says "because the
  80th percentile means she beat 80 percent of them".
- **The crowd had no centre at 7 (HIGH).** The outlier plot was 6, 7, 7, 8, 8, 9, 26 — two
  equal stacks, no single mode, and 19 was the distance from a number that was neither the
  mean nor the median. The plot is now **6, 7, 7, 7, 8, 8, 9, 26**: one tallest stack at 7,
  so "the mode is 7" and "19 is the distance back to it" are both true as spoken. `wq`
  wrote that sentence and its pin; the pin moved with the fix.

Nine more rules of thumb got the condition they were stated without — the histogram's
heights ("on these histograms every height is a count"), the average distance ("when the
distances differ"), the line through the cloud ("when no dot lands on the line"), the AND
rule ("for chances like these, with neither one certain"), the winning paths ("for paths
that win BOTH times"), the 0-to-100 scale ("the chances that sometimes happen live
between"), the distribution ("one rule every distribution keeps"), the bag that remembers
("when the marble kept out is red"), and the poll's margin ("at that size such a poll
reports a margin near 3 points").

## Boards, and the rest

`dcnt`'s two worked plots carry `mark=` so the line the words point at is drawn; the mode
teach redraws its dot plot instead of talking about dots over step lines; the sample
question draws the school it quotes; the winning paths draw `4 × 4 = 16`; the hundred draws
`25% + 40% = 65%` and writes percent on every term; `merr` draws its doubling ✗. **`resd`'s
board now writes the signed residual** — `actual 5 − predicted 8 = −3` beside `size of the
residual = 3` — so the spoken rule "actual take away predicted" and the board agree even
when the dot sits below the line. `indp`'s 34-word ask is three sentences; `resp`'s
walk-back is short sentences and draws the silent count it names; `sblw`'s walk-back is
three steps; the machine's advance line says "What you pay take away what comes back";
`iqrw`'s 20 is the **WIDTH** of the middle half; a table's box is a box, not a cell.

## Counts

Course lines **40,005 → 39,970**, closure 40,259 → 40,224, speechmap 1,001 of 40,276,
forSpeech drift 695 (unchanged). The 35 lines went because **a credit line is shared by
more problems than an explanation was** — 1,559 old praise and walk-back lines out, 1,524
in. Every lesson validates; the referee sweep over all ten courses is unchanged (the one
Basic intro card); `xe`'s and `xf`'s two measurements still read zero everywhere; the worst
per-lesson closure is `calc-u1-mend-the-curve` at 24,838 (tripwire 25,000).

PART **3nb** (19 checks): the length measurement, pinned at zero for Prob/Stat and as a
**falling ratchet** for the canon (83 → 59 — Calculus 32, Pre-Calc 19, eight between
Algebra I, Geometry and Algebra II); the six named ops by text and their walk-backs; the
six HIGHs; the nine scoped laws; the boards; the counts and notes. Six older pins moved.

## One more thing the battery caught

`main.py`'s header passed 100 KB with this build's note — the trap PART 3ke exists for, and
the one the 09-16 handoff predicted ("main.py is at ~95 KB — it goes next"). Rolled out at
cutoff **2026-09-10**, the same cutoff `wp` used for `ruletests.py` and `lessonscripts.py`:
91 entries moved into `changelog/main.py.md` verbatim, 61 kept, header 100,069 → 37,219 B.
The tool proves the old header equals the new one with the moved lines put back before it
writes anything, and 3ke re-reads every block against its own cutoff.

## Battery

Frozen copy, 2026-09-21: **12,778 passed · 0 failed · 3 skipped** (12,759 at `xf`). The first run failed one check — `main.py`'s header at 100,068 B — and nothing else; the roll-out above fixed it and the second run was clean. No pin failed on the edits themselves: every `in spoken(E(...))`, `PR(...)`, `W(...)`, `S(...)` and `B(...)` pin was scanned against the rebuilt lessons and generator before the battery ran, and the eight it found were moved first.

I did no harm and this file is not truncated.
