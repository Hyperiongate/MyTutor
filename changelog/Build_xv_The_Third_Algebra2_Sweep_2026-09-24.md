# Build xv — The Third Algebra II Sweep, 2026-09-24

Sweep header, 2026-09-23 23:46 UTC, read by gpt-5.5 on build
`2026-09-23xt-the-voice-cache-reclaim-card`: **36 of 36 lessons read · 28 findings (1 on
generators, 27 on authored beats) · 19 lessons clean · 0 unplaced · 0 unread · 2669.0s.** By
kind: false 12, words-board 10, unsupported 3, unclear 2, repeats 1. The floors: `ww`'s
reading **43 findings, 14 clean** (fixed in `wx`); `wk`'s **73**.

Stamp: **`2026-09-24xv-the-third-algebra2-sweep`**. PART **3nq**. A sweep build (the fifth of
the third round).

## The shape of the pile

**Nineteen clean of thirty-six** — the most any course has had since Entry's 29. The classes
this course keeps showing are the ones the handoff predicted for it: **the law stated for
today's numbers as if it were the whole truth** (twelve "false" findings, ten of them a
missing condition) and, arriving here from Diffeq, **the board that skips the arithmetic the
words say** (the absolute-value lesson's three beats say "negative 5" over a board that goes
straight from |3 − 8| to 5). Nothing in it was a new class.

## The three HIGHs

- **"Numbers built with i are called imaginary."** 2 + 3i is built with i and is not
  imaginary. Now: "A real number times i, like 3 i, is called an imaginary number."
- **"A real-number square can never be negative — until i."** i is not a real number; it is
  not an exception to that rule, it is a new number outside it. Now: "…can never be negative,
  so a new number was invented: i, whose one job is i squared equals negative 1."
- **"Its sign counts the crossings with the x line."** A zero discriminant is one touch, not
  a crossing — and the lesson says so two beats later. Now: "…says how the curve meets the x
  line: two crossings, one touch, or none."

## The one generator

`imag`'s praise was the walk-back's check said first — "4 i times 4 i equals 16 times i
squared — negative 16. So x is 4 i" — and the sweep called the walk-back a repeat. The `xq`
ruling: when a sweep says "repeats" after a right answer, read the *praise*. It is a credit
line now: "4 i — the i carries the minus, and 4 times 4 is 16." The walk-back keeps the check.

## Laws with their condition (10)

Degrees add *in that timesing*; every different factor *of the form x take away a number*
donates one root; roots go under one roof *for numbers that are not negative*; exponents add
for powers *of the same base*, and logs add *for positive numbers, in one log base*; a power
question turns around *for the positive bases we use*; the slots multiply *when every choice
fits every combination so far* (the why and the recap); "a mirror is not a spin" became "360
minus the angle is a mirror, not one more full turn"; the wiggle count's "why one fewer" is
now honestly *a rule for now — the full proof waits for calculus*; and "18 is nearer 16, so its
log is closest to 4" says *that is an estimate, and a fair one: the exact log is a little past
4*.

## Words and board (9)

The absolute-value lesson draws `3 − 8 = −5`, `4 − 9 = −5` and `5 − 11 = −6` before the bars
drop the sign (three beats — the `xs` class, in this course's first lesson). The forbidden-x
picture says "never touches that line" over a graph with no line: `lines="x=4"` draws it now
(rendered headless; the legend names it). The test-number recap said "the test number is
this" over a bare graph: the formula line is on the recap board, and "the x part" is "the
number in front of x" in the why and the recap both. `20 × 20 = 400` is on the halving-trap
board. The new-number recap's caption reads `x = ±3i — today, 3i`, and the picture beat says
why −3i works too ("squaring wipes a minus, so both give negative 9"). The bananas reason
question reads its two trips instead of "trips of 14 and 8 cents". "The x sits UNDER the 20"
over a `20 ÷ x` board is "the x sits after the divide sign". "The vertex lesson taught you
that flip", after a wrong choice, is "the vertex lesson's flip does not belong here".

**Declined (1):** the cosine lesson's reason choices "are never read aloud". Tap buttons by
design — this is the fourth sweep to raise it (wx, xa, xb, now) — so `coursesweep.py` carries
a charter line for it from this build: the reviewer is told, and it should not come back.

Three rewritten sentences ran to 27–35 words and were split before the battery (one caught by
`L.validate`, two by the 3na scan in the smoke run). Grading under the 09-22 ruling: **class**
(the condition, 10; the skipped arithmetic, 3), **one-off** (14), **refused** (1).

## Counts

Course lines **40,495 — unchanged**. Speechmap 941. Every lesson validates; 3na, 3nb, 3nd,
3ne, 3nn, 3eu, 3mo, 3kt and 3c pass on the edited tree. `tools/pinscan.py` found five stale
pins before the first battery (3mf ×4, 3mr ×1), all moved.

## After the push

`/health` = `2026-09-24xv-the-third-algebra2-sweep`. **Prewarm ~30 lines.** The next build is
the gate build — the child-mode skin (#7). The sweep after that: Basic 41, Geometry 36, or
Prob/Stat's second half once the seat is topped up.

## Files

`lessons/algebra2.py` (28 edits), `lessonscripts.py` (imag's praise), `coursesweep.py` (the
charter line), `ruletests.py` (PART 3nq; five pins moved), `speechmap.py` (regenerated, 941),
`main.py` (stamp), this doc, the refreshed `START_HERE_Handoff_2026-09-24.md`.

Battery on the frozen copy, 2026-09-24: **12,999 passed · 0 failed · 3 skipped** (12,991 at `xu`), first run clean.

I did no harm and this file is not truncated.
