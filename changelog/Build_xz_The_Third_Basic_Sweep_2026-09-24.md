# Build xz — The Third Basic Sweep, 2026-09-24

Sweep header, 2026-09-24 15:41 UTC, read by gpt-5.5 on build
`2026-09-24xx-the-child-mode-skin`: **36 of 36 lessons read · 9 findings (0 on generators, 9
on authored beats) · 29 lessons clean · 0 unplaced · 0 unread · 1739.9s.** _Resumed after a
restart: 4 lessons read before it, 32 now._ By kind: false 4, words-board 2, unclear 1,
untaught-term 1, unsupported 1. The floors: `wy`'s reading **41 findings, 14 clean**; `wg`'s
**67, 7**.

Stamp: **`2026-09-24xz-the-third-basic-sweep`**. PART **3nu**. A sweep build (built after
the `xy` gate build; the sweep ran while `xy` was being built).

## The smallest pile any course has produced

Nine findings, twenty-nine clean — and no generator finding at all, the first reading of any
course with none. Basic went 67 → 41 → 9.

## Two of the nine were the reviewer's, and both became tool fixes

**"Transcript metadata spoken as a tutor line."** The times-table lesson's transcript ends
with the sweep's own marker — *(the times-table pass continues for all 81 facts; the first 6
are shown)* — and `render_transcript` labelled it `TUTOR:` like every other beat. The reviewer
was right to object; the label was wrong. A `note` turn reads *(transcript note — never spoken,
not the tutor's)* now, and the charter says a line so marked is never a finding.

**48 ÷ 3 (two HIGHs).** The reviewer read the PROBLEM SPACE line — *a is one of 22 … 48; b is
one of 2, 3, 4, 5* — as a cross product, paired 48 with 3, and called the tens-and-ones split
false. The lesson asks 48 ÷ 4; every one of its twelve problems splits cleanly (PART 3nu checks
that against the bank). The line invited the misread, so when the space is small enough to
list, it now lists the exact pairs too: *the exact problems (a, b): (22, 2), (26, 2), … (48, 4)
…*, and the charter says a pair the list does not contain is not a finding. The `wh` and `wz`
pins on the line's shape still hold.

That said, the *class* under the two HIGHs is real — the condition class the third round keeps
finding — and is fixed: the teach says the pieces are *"today, tens and ones, because each
piece divides with nothing left over"*, and the recap *"Here that was the tens and the ones,
because each piece divided with nothing left over."*

## The condition class (3) and the one-offs (5)

*"Every number lives between two tens"* (40 does not) → *"A number that is not a ten itself
sits between two tens."* The two division lines above.

The left-overs recap said *"then take that away"* — *that* is *"the number of things in those
full groups"* now, in its own sentence. LCM is named before the board writes it (*"We write
least common multiple as LCM for short."*). The equivalent-fractions practice intro told the
child to *"look at the two pies"* over the Your-turn card — *"Each question brings two pies to
look at."* The simplest-form intro said *"find what the top and bottom share"* — *"the biggest
number … and divide both by it"*; and its recap's second sentence (*"if they still share
something, divide again"*) did not follow from the first (after the biggest, nothing is left to
share) — *"If you used a smaller shared number, they may still share something — divide
again."*

Nothing declined. Grading under the 09-22 ruling: **class** (the condition, 3), **one-off**
(4), **reviewer** (2 — both turned into tool fixes rather than refusals).

## Counts

Course lines **40,495 — unchanged**. Speechmap 941. Every Basic lesson validates. Two pins
moved with the text (the rounding closure line in the voice-miss head test; the simplest-form
recap in 3mf). `tools/pinscan.py` clean (its one report is 3nu's own negative pin).

## After the push

`/health` = `2026-09-24xz-the-third-basic-sweep`. **Prewarm ~8 lines.** The next sweep: Geometry
36, Calculus 46 (its third), Entry 19, or Pre-Calc's fourth. The next gate build: the over-tall
beat that `xy`'s instrument counts (29 LOW S9s at 1280×900).

## Files

`lessons/basic.py` (8 edits), `coursesweep.py` (the transcript note; the exact pairs; the
charter), `ruletests.py` (PART 3nu; two pins moved), `speechmap.py` (regenerated, 941),
`main.py` (stamp), this doc, the refreshed `START_HERE_Handoff_2026-09-24.md`.

Battery on the frozen copy, 2026-09-24: **13,073 passed · 0 failed · 3 skipped** (13,061 at `xy`), first full run clean.

I did no harm and this file is not truncated.
