# =============================================================================
# requirements.txt  --  Math Tutor MVP  --  Hyperion Shift LLC
# CHANGE NOTES (keep newest at top):
#   2026-07-21  Pinned Python to 3.12 via a `.python-version` file in the repo root.
#               Render now defaults new services to Python 3.14, and psycopg2-binary
#               2.9.9 has no working build for 3.14 (ImportError: undefined symbol
#               _PyInterpreterState_Get), which kept the DB disabled. 3.12 is fully
#               supported by every dependency here.
#   2026-07-21  Added SQLAlchemy + psycopg2-binary for the new durable storage layer
#               (store.py). These are only USED when the DATABASE_URL env var is set
#               (e.g. a Render PostgreSQL instance); with no DATABASE_URL the app
#               still runs on JSON files exactly as before. Pinned to known-good
#               versions so a redeploy can't pull a breaking update.
#   2026-07-20  Added python-multipart (needed for the /api/transcribe file upload
#               / ElevenLabs speech-to-text). fastapi also pulls it transitively,
#               but pinning it explicitly is safer.
#   2026-07-19  Upgraded anthropic 0.34.2 -> 0.117.0 to fix a hard startup crash:
#               the old SDK passed a 'proxies' argument that newer httpx (0.28.x)
#               removed, so the Claude client could not be constructed and every
#               chat failed with "Client.__init__() got an unexpected keyword
#               argument 'proxies'". Also pinned httpx==0.28.1 so the anthropic
#               and httpx versions stay a known-good, tested pair on every deploy.
#   2026-07-19  Initial Day 1 dependencies. Pinned to versions known to work
#               together so a redeploy does not silently pull a breaking update.
# NOTE: pip ignores lines starting with '#', so these notes and the closing
#       statement below are safe to keep in this file.
# =============================================================================
fastapi==0.111.0
uvicorn[standard]==0.30.1
anthropic==0.117.0
httpx==0.28.1
python-multipart==0.0.9
SQLAlchemy==2.0.34
psycopg2-binary==2.9.9
# I did no harm and this file is not truncated.
